from setuptools import setup, find_packages

setup(
    name="orbitalsense-pipeline",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[],
    py_modules=["transforms", "validation"],
)
